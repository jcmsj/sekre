import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    row: {
        width: "100%",
        display: "flex",
        flexDirection:"row"
    },
});

export default style;